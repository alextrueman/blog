on:
  pull_request:
    branches:
      - master

name: rubocop

jobs:
  rubocop:
    name: rubocop
    runs-on: ubuntu-latest

    steps:
      - name: Rubocop checks
        uses: gimenete/rubocop-action@1.0
        env:
          GITHUB_TOKEN: ${{ secrets.RUBOCOP_SECRETT }}
